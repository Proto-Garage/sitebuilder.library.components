import * as React from "react";
import { Styles } from "sitebuilder.client";
interface IProps {
    styles?: Styles | null;
    boxed?: boolean;
}
declare const Topbar: React.SFC<IProps>;
export default Topbar;
